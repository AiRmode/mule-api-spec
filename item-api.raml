#%RAML 1.0
title: item
version: v1
protocols:
  - HTTP
baseUri: /api

/item:
  get:
    queryParameters:
      sku:
        type: string
    responses:
      200:
        body:
          application/json:
            type: !include person.json
      400:
        body:
          